<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.neobis">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Diabetik">
        <activity android:name=".ui.activities.registration.ConfirmNewAccountActivity"></activity>
        <activity android:name=".ui.activities.registration.CreateAccountActivity" />
        <activity android:name=".ui.activities.forgottenPassword.SetNewPasswordActivity" />
        <activity android:name=".ui.activities.forgottenPassword.ConfirmResetCodeActivity" />
        <activity android:name=".ui.activities.forgottenPassword.ForgottenPasswordActivity" />
        <activity android:name=".ui.activities.AuthorizationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.SplashActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.activities.MainActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>